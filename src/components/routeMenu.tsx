interface routeMenuProps {
  //mapChild: { children: (string | Element)[] };
}
export const RouteMenu = (props: routeMenuProps) => {
  return <div style={{ color: "blue", zIndex: 1 }}>HELLO</div>;
};

export default RouteMenu;
